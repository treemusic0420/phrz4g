name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          # Use LTS for maximum action/dependency compatibility in CI.
          node-version: 20

      - name: Create .env for Vite build
        run: |
          cat <<ENVEOF > .env
          VITE_ALLOWED_EMAIL=${{ secrets.VITE_ALLOWED_EMAIL }}
          ENVEOF

      - name: Validate VITE_ALLOWED_EMAIL value (no secrets output)
        run: |
          node -e "
          const value = process.env.VITE_ALLOWED_EMAIL;
          const exists = !!value;
          console.log('VITE_ALLOWED_EMAIL_EXISTS=' + exists);
          if (!exists || value === 'undefined') {
            throw new Error('VITE_ALLOWED_EMAIL is missing or invalid');
          }
          "
        env:
          VITE_ALLOWED_EMAIL: ${{ secrets.VITE_ALLOWED_EMAIL }}

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Create Firebase service account file
        run: |
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PHRZ4G }}' > "$RUNNER_TEMP/firebase-service-account.json"

      - name: Deploy to Firebase Hosting (live)
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ runner.temp }}/firebase-service-account.json
        run: npx firebase-tools deploy --only hosting --project phrz4g
